# Oblivion

Facts about this modlist. Keep it short and keep it true. Anything longer than a
paragraph belongs in `docs/research/`, anything dated belongs in
`changelog.md`, anything broken belongs in `issues.md`.

## Install

| field | value |
|---|---|
| Edition | not recorded, one of Oblivion GOTY, Oblivion Remastered |
| Game path | not recorded |
| MO2 instance | not recorded |
| MO2 version | not recorded |
| Live profile | not recorded |
| Script extender | not recorded, OBSE or OBSE64 |
| Engine notes | not recorded, which executable is being launched and which script extender build matches it |
| Started | not recorded |

Fill this in from the first snapshot, then keep it current.

## Hardware

Not recorded. GPU, VRAM, CPU, RAM and which drive the install sits on. This
decides what texture and ENB choices are even on the table, so it is worth
having written down.

## What this list is for

Not recorded. One paragraph. What kind of playthrough is this build serving,
what is deliberately out of scope, and what the visual and gameplay target is.
Write it now because in eight months it stops being obvious, and every later
decision gets argued against it.

## Snapshots

Newest folder in `snapshots/` is the current recorded state. Each one holds
`SUMMARY.md` for reading, `snapshot.json` for machines, and `raw/` with the
untouched MO2 profile files.

Snapshots are immutable. If something changed, take a new one.
